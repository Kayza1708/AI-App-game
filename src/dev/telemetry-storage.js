import { cloneState } from '../core/clone.js';
const STORAGE_KEY='ai-singularity-dev-sessions';const ACTIVE_KEY='ai-singularity-active-telemetry';const MAX_SESSIONS=10;
export class TelemetryStorage { constructor(storage=globalThis.localStorage){this.storage=storage;this.warning=null}list(){try{return JSON.parse(this.storage?.getItem(STORAGE_KEY)??'[]')}catch(error){this.warning=error.message;return[]}}save(session){try{const sessions=this.list();const entry=cloneState(session);const existing=sessions.findIndex(s=>s.id===entry.id);if(existing>=0)sessions.splice(existing,1);sessions.unshift(entry);const pinned=sessions.filter(s=>s.pinned),recent=sessions.filter(s=>!s.pinned).slice(0,MAX_SESSIONS);this.storage?.setItem(STORAGE_KEY,JSON.stringify([...pinned,...recent]));return true}catch(error){this.warning=`Telemetry persistence failed: ${error.message}`;return false}}loadActive(){try{return JSON.parse(this.storage?.getItem(ACTIVE_KEY)??'null')}catch{return null}}saveActive(value){try{this.storage?.setItem(ACTIVE_KEY,JSON.stringify(value));return true}catch(error){this.warning=error.message;return false}}clearActive(){try{this.storage?.removeItem(ACTIVE_KEY)}catch{}}delete(id){this.#write(this.list().filter(s=>s.id!==id))}pin(id,value=true){this.#write(this.list().map(s=>s.id===id?{...s,pinned:value}:s))}clear(){try{this.storage?.removeItem(STORAGE_KEY);this.storage?.removeItem(ACTIVE_KEY);this.warning=null;return true}catch(error){this.warning=error.message;return false}}#write(value){try{this.storage?.setItem(STORAGE_KEY,JSON.stringify(value))}catch(error){this.warning=error.message}}}
export { ACTIVE_KEY,STORAGE_KEY,MAX_SESSIONS };
