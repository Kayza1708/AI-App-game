export class DowntimeAnalyzer {
  constructor({warning=30,significant=60,severe=120}={}){this.thresholds={warning,significant,severe};this.periods=[];this.active=null}
  update({seconds,lastActionSeconds,backgrounded,snapshot,lastAction}){const idle=seconds-lastActionSeconds;const shouldStart=!backgrounded&&idle>=this.thresholds.warning&&snapshot.usefulPurchases===0&&snapshot.claimableRewards===0&&!snapshot.strategicChoice;if(shouldStart&&!this.active)this.active={start:seconds-idle,end:null,duration:0,severity:'warning',cause:cause(snapshot),bottleneck:snapshot.bottleneck,state:{credits:snapshot.credits,research:snapshot.research,energyEfficiency:snapshot.energyEfficiency},nearestPurchase:snapshot.cheapestUsefulLabel,estimatedWait:snapshot.cheapestUsefulEta,before:lastAction?.type??null,after:null};if(this.active){this.active.duration=seconds-this.active.start;this.active.severity=this.active.duration>=this.thresholds.severe?'severe':this.active.duration>=this.thresholds.significant?'significant':'warning'}if(this.active&&(!shouldStart||backgrounded)){this.active.end=seconds;this.active.after=lastAction?.type??null;this.periods.push(this.active);this.active=null}}
  close(seconds,lastAction){if(this.active){this.active.end=seconds;this.active.duration=seconds-this.active.start;this.active.after=lastAction?.type??null;this.periods.push(this.active);this.active=null}}
}
function cause(s){if(s.energyEfficiency<.8)return'waiting-for-Energy';if(s.trainingActive)return'waiting-for-Training';if(s.bottleneck==='Demand Limited')return'Demand-limited';if(s.bottleneck==='Capacity Limited')return'Capacity-limited';if(s.researchRate<.05)return'waiting-for-Research';if(s.cheapestUsefulEta>0)return'waiting-for-Credits';return'no-affordable-upgrade'}
